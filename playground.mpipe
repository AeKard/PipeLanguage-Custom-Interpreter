faucet multiplicationTable(l, r){
    cycle(l < 5)
    {
        r = 0;
        cycle(r < 5){
            r = r + 1;
            flow("l:", l, " * ","r:", r, " = ", l*r);
        }
        l = l + 1;
        flow("l :",l);
    }
}
multiplicationTable(1,1);